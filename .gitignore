node_modules/
dist/
build/
.DS_Store
*.local

# Claude Code local state (machine-specific, not shared)
.claude/settings.local.json
.claude/shell-snapshots/

# Playwright MCP debug snapshots/screenshots (local scratch output)
.playwright-mcp/

# Claude Code project guidance (local-only, not shared via the repo)
CLAUDE.md
